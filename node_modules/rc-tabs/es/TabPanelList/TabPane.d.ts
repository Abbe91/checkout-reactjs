/// <reference types="react" />
import { Tab } from '../interface';
export interface TabPaneProps {
    prefixCls: string;
    id: string;
    tab: Tab;
    animated: boolean;
    active: boolean;
    destroyInactiveTabPane?: boolean;
}
export default function TabPane({ prefixCls, id, active, animated, destroyInactiveTabPane, tab: { key, children, forceRender, className, style }, }: TabPaneProps): JSX.Element;
